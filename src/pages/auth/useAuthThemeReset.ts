/**
 * Auth screens dynamically reflect the user-selected theme configured in Settings.
 * Retained for backward-compatibility with existing imports without forcing static overrides.
 */
export function useAuthThemeReset() {
  // Dynamic theme support: do not force static override class so active theme colors persist
}
